import 'package:auto_styles/app.dart';
import 'package:jaspr/jaspr.dart';
import 'package:jaspr/styles.dart';

const smallText = Styles.text(color: Colors.aqua, fontSize: Unit.pixels(40));

var otherText = Styles.text(color: Colors.aqua, fontSize: Unit.pixels(40));

void main() {
  runApp(App());
}
